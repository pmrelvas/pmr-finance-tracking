quarkus:
  http:
    port: ${HTTP_PORT:8080}
    cors:
      enabled: true
      origins: ${CORS_ORIGINS:http://localhost:4200}
  mongodb:
    connection-string: ${MONGODB_URL:mongodb://root:password@localhost:27017/finance_tracker?authSource=admin}
    database: ${MONGODB_DATABASE:finance_tracker}